version: "3.8"

services:
  fastapi:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app

  tensorboard:
    build: .
    command: tensorboard --logdir=logs --host=0.0.0.0 --port=6006
    ports:
      - "6006:6006"
    volumes:
      - .:/app
